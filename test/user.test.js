import app from "../app.js";
/* import { Request } from "supertest";
import { expect } from "chai"; */

describe("test on /user", () => {
  describe("test on /user/read", () => {
    it("deberia traer 200 si trae usuarios", async () => {});
  });
});
